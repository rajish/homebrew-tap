# Homebrew Tap

Homebrew tap for [cc-hdrm](https://github.com/rajish/cc-hdrm) — a macOS menu bar utility showing Claude API usage headroom.

## Install

```bash
brew tap rajish/tap
brew install --cask cc-hdrm
```

## Upgrade

```bash
brew upgrade --cask cc-hdrm
```

## Uninstall

```bash
brew uninstall --cask cc-hdrm
```
